// const Joi = require('joi');

// const id = Joi.number().integer();
// const description = Joi.string().max(255);
// const dewormer = Joi.string().min(1).max(255);

// const dewormingIdSchema = Joi.object({
//   dewormingId: id.required(),
// });

// const createDewormingSchema = Joi.object({
//   dewormer: Joi.string().min(1).max(255).required(),
//   description: Joi.string().optional(),
//   animalId: id.required(),
// });

// const updateDewormingSchema = Joi.object({
//   dewormer: dewormer.required(),
//   description: description.optional(),
// });

// module.exports = {
//   dewormingIdSchema,
//   createDewormingSchema,
//   updateDewormingSchema,
// };
